name: test

on:
  push:
    branches: master

jobs:
  job1:
    name: job1
    runs-on: ubuntu-18.04
    steps:
    - name: checkout repo
      uses: actions/checkout@v1
      with:
        fetch-depth: 1
    - name: echo Hello, World
      run: echo Hello, Mark Donile!